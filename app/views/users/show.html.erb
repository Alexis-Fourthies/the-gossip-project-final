<div class="div_text_centered">
 <h1>Profil de <%= User.all.find_by(id: @id).first_name %></h1>
  <h2>Prénom</h2>
    <p><%= User.all.find_by(id: @id).first_name %></p><br/>
  <h2>Nom</h2>
    <p><%= User.find(@id).last_name  %></p><br/>
  <h2>Âge</h2>
    <p><%= User.find(@id).age %></p><br/>
  <h2>Description</h2>
    <p><%= User.find(@id).description %></p><br/>
  <h2>Email</h2>
    <p><%= User.find(@id).email %></p><br/>
  <h2>Ville</h2>
    <p><%= User.find(@id).city.name %></p><br/>

  <%= link_to "Ville de #{User.all.find_by(id: @id).first_name}", city_path(User.find(@id).city.id), class:"btn btn-primary" %>

  <hr>
</div>